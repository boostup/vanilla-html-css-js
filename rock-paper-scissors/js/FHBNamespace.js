(function(win, doc) {
  
  "use strict";

  /**
   * Why FHB ? FB = Facebook, so => Fred H. Beauvois => FHB
   * All domain objects will be hooked into this object.
   */
  if(!win.FHB) win.FHB = {};

})(window, document);